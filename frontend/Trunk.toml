[build]
target = "index.html"
dist = "../frontend-dist"
public_url = "/"

[[proxy]]
backend = "http://localhost:3000/api/*"
rewrite = "/api/*"

[[proxy]]
backend = "http://localhost:3000/v1/models"
rewrite = "/v1/models"
